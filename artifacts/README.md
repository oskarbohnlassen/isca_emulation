# Artifacts

This directory previously contained local evaluation artifact folders for downloaded W&B model checkpoints.
Each folder contained `evaluation_results.pt` and an `[model_name]_*.pt` checkpoint. These files are generated or downloaded locally and are not tracked in Git.
