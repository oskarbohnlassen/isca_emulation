# W&B Local State

This directory previously contained local Weights & Biases runtime state:

- `debug.log`
- `debug-internal.log`
- `latest-run`
- `run-20260527_*` folders, each with local W&B `files/`, `logs/`, and `tmp/` subdirectories.

These files are machine-specific logs and run metadata, so they are not tracked in Git.
