# Data

This directory previously contained local Isca data inputs and processed tensors:

- `raw/`: symlinks to the large Isca simulation outputs.
- `processed/`: preprocessed CNN, grid-GNN, and mesh-GNN datasets used by training and evaluation.

The data files are large local research artifacts and are not tracked in Git.
